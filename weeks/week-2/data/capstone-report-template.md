# Week 2 capstone report — the scoped docs layer, measured

Paper fallback for the deck's export. Fill it as you go; paste the result on your
Week 2 Knowledge issue.

## Target

Repo or workspace, its shape (monorepo / multi-repo / single), and why you picked it.

## The suite and predictions

The five tasks (from task-suite-template.md) and which ones you predicted the layer
would improve.

## Baseline (no docs layer)

Per task: questions asked, wrong files or wrong assumptions, instructions repeated,
context used at the end.

## The layer I authored

Files written, one line each, with the scope you chose and the argument for it:

- `~/.claude/CLAUDE.md` (global):
- `CLAUDE.md` (project root):
- Directory or workspace file:
- `CONTEXT.md`:
- `guardrails.md`:
- `AGENTS.md` symlink: created with, verified by

## A/B deltas

Per task: what changed against baseline, with the evidence line. Then the honest
summary: where the layer paid, where it made no difference, where it hurt.

## Verdict

- The delta that surprised me most:
- One rule I will carry into every repo I own:
