{
  "name": "relay-mono",
  "version": "0.4.0",
  "private": true,
  "description": "Relay as a monorepo: core owns storage, cli and report consume it.",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "node --test packages"
  },
  "license": "MIT"
}
