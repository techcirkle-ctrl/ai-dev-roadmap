# relay workspace

Relay outgrew one repo. It is now three, checked out side by side in this folder:

- `relay-api` owns the data file and serves it over HTTP
- `relay-cli` is the team's terminal front door
- `relay-report` renders the weekly shipping report

Each repo has its own README. Start with relay-api.
