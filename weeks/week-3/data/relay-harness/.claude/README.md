Deliberately empty.

Building this folder is the point of week 3: a settings file with permission rules,
two commands, two hooks and one skill. Start with `guardrails.md` in the repo root and
pick the rule you least want anyone to ignore.
